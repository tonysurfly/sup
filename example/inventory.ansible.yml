# Example Ansible Inventory for Sup
all:
  children:
    webservers:
      hosts:
        web1.example.com:
        web2.example.com:
          ansible_host: 192.168.1.102 # Example of host variable
    dbservers:
      hosts:
        db1.example.com:
  vars:
    common_var: "this is common to all"
ungrouped:
  hosts:
    utility1.example.com:
    192.168.3.3: # Host without a name, will use IP
