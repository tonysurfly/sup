---
# Example Ansible inventory file for Sup
all:
  children:
    webservers:
      hosts:
        web1.example.com:
        web2.example.com:
          ansible_host: 192.168.1.102
    dbservers:
      hosts:
        db1.example.com:
  hosts:
    utility1.example.com:
    192.168.3.3:
  vars:
    common_var: "this is common to all"
